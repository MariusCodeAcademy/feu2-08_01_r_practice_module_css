import Container from '../UI/container/Container';
import Section from './../UI/section/Section';

function GainMore(props) {
  return (
    <Section>
      <Container>Gain more section title</Container>
    </Section>
  );
}
export default GainMore;
